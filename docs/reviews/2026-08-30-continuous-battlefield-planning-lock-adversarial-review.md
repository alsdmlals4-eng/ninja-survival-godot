# 2026-08-30 continuous-battlefield planning lock — adversarial review

## Scope

This review covers only the user-locked `SCRREF-BATTLE-AUTOCOMBAT-03` planning
reference, its repository provenance, and its human-blueprint/PDF projection.
It excludes Godot scene, texture, prop spawning, collision, runtime rendering,
and player validation implementation.

## Five whole-state re-attacks

| Loop | Attack | Evidence | Result |
| --- | --- | --- | --- |
| 1 | Could a planning image silently replace the approved runtime background? | `git diff --exit-code -- assets scenes project.godot` returned success; decision and handoff name the existing `Main/BattlefieldBackdrop` as unchanged. | PASS — no runtime surface changed. |
| 2 | Is the locked image only a chat artifact without durable provenance? | Repository PNG SHA-256 is `68727c87b5f81dee18f06bb0955d37314a3e0ec03f04fe9dd33f842df0dd6eac`; the screen-reference manifest records user lock, producer, status, and consumer. | PASS — repository source and provenance are present. |
| 3 | Could the GDD and manifest disagree about what consumes the image? | PDF manifest hash checks passed for GDD source, generated PDF, and locked PNG; both declare blueprint page 10/PDF page 10 as the human consumer and no runtime consumer. | PASS — consumer boundary agrees. |
| 4 | Could the revised page clip Korean copy or hide the visual boundary? | PDF has 28 A4 landscape pages, 28 nonblank extracted-text pages, and rendered PNGs. Pages 10, 25, and 28 were re-inspected at high detail. | PASS — no observed clipping, overlap, or missing Korean glyphs on changed pages. |
| 5 | Did the lock accidentally expand into combat, UI, or product-rule changes? | Full `tools.test_export_human_gdd_pdf` passed 8/8; final diff is limited to visual handoff, publication manifest, and regenerated PDF after source/decision/reference commit. | PASS — no combat, route, economy, backpack, or Godot behavior changed. |

## Validated corrections

- A first staged whitespace check identified four Markdown trailing-space
  findings in DEC-038. They were removed, then the full PDF test suite and
  `git diff --check` were rerun successfully before the final publication
  artifact was prepared.
- The publication manifest was refreshed from the generated bytes rather than
  copied from the previous PDF: source SHA-256, PDF SHA-256, timestamp,
  byte size, and 8/8 test record now match the locked revision.

## Outcome and evidence ceiling

`SCRREF-BATTLE-AUTOCOMBAT-03` is fit for the declared human-blueprint role:
`USER_LOCKED_PLANNING_REFERENCE_NOT_RUNTIME`. This review does not prove tiled
background code, separate prop resources, Godot import, live scene rendering,
Human Usability, Player Experience, touch/gamepad, device/export, or release
readiness. Those require a separate runtime package with a fresh consumer
contract.
